import React from 'react'
import { <%= name %> } from './<%= name %>'

export default {
  component: <%= name %>,
  title: '<%= name %>',
}

export const Example = (): JSX.Element => <<%= name %> /> 
